# Optional: only when frontend and backend are on different domains (Vercel + Render).
# Vercel → Project → Settings → Environment Variables (Production)
# Example:
# VITE_API_BASE_URL=https://canela-coach-api.onrender.com/api
VITE_API_BASE_URL=
